include mixins.pug

html
  +header()
  body
    +menuTemplates(categories)
    div.container
      +newTemplateForm
      +templatesList(templates)
    div.floatingModuleContainer(id="addTemplateComponentHolder" class="moduleHolder")
      +addTemplateComponentForm(categories)
